version: "3.9"
services:
  # web:
  #   build: .
  #   ports:
  #     - "5000:5000"
  # redis:
  #   image: "redis:alpine"
  ffc-experiment-local:
    build:
      context: ../FeatureFlagsCo.Experiments
      args: 
        exptenv: local
      dockerfile: Dockerfile
    image: ffc-experiments
    profiles: 
      - local
    networks:
      - featureflagsco_ffc
    volumes:
      - ./log:/var/log/

networks:
  featureflagsco_ffc:
    external: true


